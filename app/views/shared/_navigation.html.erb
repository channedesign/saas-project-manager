<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to('SaasProjectManager', root_path, class: "navbar-brand") %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
    	<ul class="nav navbar-nav">
    		<% if current_user && current_user.is_admin? %>
    			<li>
    				<%= link_to('<i class="fa fa-users" aria-hidden="true"></i> Add Members'.html_safe, new_member_path, class: "btn btn-primary btn-xs") %>
    			</li>
    			<li>
    				<%= link_to('<i class="fa fa-briefcase" aria-hidden="true"></i> Change Plan'.html_safe, edit_plan_path, class: "btn btn-primary btn-xs") %>
    			</li>
    		<% end %>
    	</ul>
      <ul class="nav navbar-nav navbar-right">
      	<% if current_user %>
      		<li><%= link_to('<i class="fa fa-user" aria-hidden="true"></i> '.html_safe + current_user.email, edit_user_registration_path, title: "Edit Profile", class: "profile_link") %></li>
        	<li><%= link_to("Log Out", destroy_user_session_path, title: "Log Out", method: :delete, class: "btn btn-danger") %></li>
        <% else %>
        	<li><%= link_to("Sign In", new_user_session_path, class: "btn btn-primary") %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>